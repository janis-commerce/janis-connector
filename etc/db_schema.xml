<?xml version="1.0"?>

<schema xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Setup/Declaration/Schema/etc/schema.xsd">
    <table name="sales_order" resource="default" engine="innodb">
        <column xsi:type="boolean" name="is_order_created_notified" nullable="false" comment="Value to save janis order created notification state"/>
        <column xsi:type="boolean" name="is_order_invoice_notified" nullable="false" comment="Value to save janis order invoice notification state"/>
    </table>

    <!-- Updating notification fields in sales_order_grid every time that changes in sales_order -->
    <table name="sales_order_grid" resource="default" engine="innodb">
        <column xsi:type="boolean" name="is_order_created_notified" nullable="false" comment="Value to save janis order created notification state"/>
        <column xsi:type="boolean" name="is_order_invoice_notified" nullable="false" comment="Value to save janis order invoice notification state"/>
    </table>
</schema>
